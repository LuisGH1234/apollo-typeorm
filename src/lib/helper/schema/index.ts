import { DocumentNode } from "graphql";

export const mutations: Resolver = {}; // Singleton
export const queries: Resolver = {}; // Singleton
export const typeDefs: DocumentNode[] = []; // Singleton
